name: core

on:
  push:
  pull_request:

jobs:
  checks:
    name: Core checks
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Restore cargo cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "crates/jepra-core"

      - name: Check formatting
        run: cargo fmt --manifest-path crates/jepra-core/Cargo.toml --all -- --check

      - name: Lint
        run: cargo clippy --manifest-path crates/jepra-core/Cargo.toml --all-targets --all-features -- -D warnings

      - name: Test all targets
        run: cargo test --manifest-path crates/jepra-core/Cargo.toml --all-targets
